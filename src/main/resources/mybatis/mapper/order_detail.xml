<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.postella.dao.Order_detailDao">

	<!-- parameterType 객체를 DB에 제공하고 싶을때! -->
	<!-- null이 들어갈 컬럼값은 반드시 jdbcType을 명시해야함 -->

	<select id="selectStarAvg" parameterType="int" resultType="int">
		SELECT
		AVG(product.prd_star_avg)
		FROM order_detail, product
		WHERE order_detail.PRD_NO = PRODUCT.PRD_NO AND
		PRODUCT.PG_NO = ${pg_no}
	</select>
	
	<select id="selectReviewCnt" parameterType="int" resultType="int">
		SELECT
		count(*)
		FROM order_detail, product
		WHERE order_detail.PRD_NO = PRODUCT.PRD_NO AND
		PRODUCT.PG_NO = ${pg_no}
	</select>

</mapper>