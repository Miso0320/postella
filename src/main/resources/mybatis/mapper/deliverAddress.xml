<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.postella.dao.DeliverAddressDao">

	<insert id="insertAddress" parameterType="DeliverAddress">
		insert into deliver_address (da_no, da_type, us_no,  sel_no, da_name, da_tel, da_postcode, da_adr, da_detail_adr, da_main)
		VALUES(da_no_seq.nextval, #{da_type}, #{us_no, jdbcType=INTEGER}, #{sel_no, jdbcType=INTEGER}, #{da_name}, #{da_tel}, #{da_postcode}, '#{da_adr}, #{da_detail_adr}, #{da_main})
	</insert>

 
 	<select id="selectAddressList" parameterType="int" resultType="DeliverAddress">
 		select * 
		from deliver_address
		where us_no = #{us_no};
 	</select>
</mapper>